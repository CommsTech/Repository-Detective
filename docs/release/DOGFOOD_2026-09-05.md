# Dogfood journal — 2026-09-05 (RD-035)

**Product:** Repository Detective  
**Operator:** Phase 8A dogfood against Repository Detective itself (sanitized/public tree)  
**Environment:** Disposable compose ports (not production `:8081`); AI **disabled**; deterministic scanners  
**Scope:** Operator path — onboarding, Doctor, scan/webhook, findings, policy, calibration, export, restart  
**Classification of this run:** Operator journal + defect intake (not a substitute for RD-017 E2E)

---

## Session setup

| Item | Value |
|------|-------|
| Source | Current `main` development candidate (post RD-031/032) |
| AI | Disabled (`ENABLE_LLM_AUDITORS=false`) |
| Forge | Disposable Gitea 1.22.3 (upgrade harness / e2e topology) |
| Issues | Prefer labeled test issues / disposable project — do not pollute production canonical issues |

---

## Flow exercised

1. Onboarding `/onboard` — connect → select → protect → verify → ready  
2. Doctor (`/api/v1/doctor`, `/ui/doctor`)  
3. Repository scan (webhook push + post-upgrade activity)  
4. Push event with synthetic secret fixture  
5. Canonical findings + forge issue mapping (where seeded)  
6. Policy / Doctor checks  
7. Finding review / FP disposition (API + UI affordances reviewed)  
8. Calibration / learning UI affordances reviewed  
9. Report/export surfaces (Doctor bundle, env export)  
10. Restart / upgrade restart idempotency (RD-033 harness)

---

## Friction & UX observations (honest)

| ID | Severity | Observation |
|----|----------|-------------|
| DF-001 | High UX | Onboarding still requires an **automation API token** early for `/api/v1/onboard/*` even when recommending local operator login — correct for API protection, but easy to confuse with “operator password.” RD-032 wording helps; still an extra mental model. |
| DF-002 | High UX | After env export, bootstrap at `/ui/bootstrap` is a separate step with no “you are here” checklist tying Ready → restart with new `.env` → bootstrap → first scan. |
| DF-003 | Normal | Doctor JSON is excellent for operators who know it exists; Ready stage links help, but the default next click after onboarding is still easy to lose. |
| DF-004 | Normal | Learning / calibration page uses dense metrics (“Avg false-positive rate”) that can be misread as product-wide accuracy — RD-024 docs now clarify disposition vs rate; UI copy still worth a follow-up pass. |
| DF-005 | Enhancement | Compact PR summary is strong once a PR exists; first-time operators may not know to open a disposable PR to see it. |
| DF-006 | Noise | Synthetic Slack fixture reliably creates findings (good); gitleaks/history toggles still surprise operators when history scan is off vs on. |
| DF-007 | Normal | Upgrade harness shows that seed scans under beta.3 can take several minutes (grype DB / scanners) — operator patience / progress UI remains a beta friction point. |
| DF-008 | Enhancement | Finding-quality metrics API added (`/analytics/finding-quality`) but no first-class UI panel yet. |

---

## Failures / runtime

- Unit/integration packages for auth, redaction, metrics: **PASS** (Phase 8A changes).  
- Upgrade harness (`scripts/e2e-upgrade-from-beta3.sh`): **PASS** — `UPGRADE_FROM_BETA3_TO_CURRENT_MAIN_INTEGRATION_PROVEN` (run `20260905T191112Z-upgrade-3452221`).  
- No production forge pollution intended.

---

## Defects filed (tracked)

Do **not** opportunistically fix all inside the dogfood run. Track:

| ID | Class | Title | Notes |
|----|-------|-------|-------|
| DF-001 | high-value UX | Clarify automation token vs operator login throughout onboarding | Partial fix in RD-032; follow-up polish |
| DF-002 | high-value UX | Post-export checklist: restart → bootstrap → first scan | Enhancement |
| DF-004 | normal bug/UX | Soften “false-positive rate” labeling on Learning page | Align with RD-024 semantics |
| DF-007 | enhancement | Scan progress / ETA for first standard-profile scan | Beta usability |
| DF-008 | enhancement | Surface finding-quality metrics in UI | After API stabilizes |

**Blockers found this dogfood:** none that prevent beta.4 RC preparation. Upgrade integration proof is established (not yet published-release→release).

---

## Scanner runtime notes

- Standard profile scanners dominate wall clock on cold grype DB.  
- Deterministic-only (AI off) is the right dogfood baseline.

---

## Follow-ups explicitly out of scope

- RD-015 / RD-016  
- Class-B remediation expansion  
- Forgejo support claims  
- Recreating Phase-7 PR screenshot for cosmetic proof  
