# Optional override: host networking for homelab deployments that require it.
# Usage: docker compose -f docker-compose.yml -f docker-compose.host-network.yml up -d

services:
  repository-detective:
    network_mode: host
    ports: []
    networks: []

networks:
  rd-net:
    external: false
