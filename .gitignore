# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (remove the comment below to include it)
# vendor/

# Go workspace file
go.work

# Build output
bin/
dist/
build/

# IDE and editor files
.vscode/
.idea/
*.swp
*.swo
*~

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Logs
*.log
logs/

# Environment files
.env
.env.local
.env.*.local
# Local operator config may contain secrets; use config.yaml.example + .env instead.
config/config.yaml

# Local SQLite databases
data/
data/*.db
*.db

# Scanner workspace temp files
.scanner-workspace/
scanner-workspace/

# Runner local artifact directories
runner-artifacts/
runner-work/

# Dogfood reports (may contain operator-specific details — keep local by default)
docs/dogfood-reports/*
# Sanitized, committable dogfood reports (no keys, tokens, or internal hostnames)
!docs/dogfood-reports/qdrant-redacted-local-test.md
!docs/dogfood-reports/qdrant-redacted-test-result.json
!docs/dogfood-reports/ruview-preinstall-audit.md
!docs/dogfood-reports/ruview-preinstall-shareable-report.md
!docs/dogfood-reports/ruview-private-security-disclosure-draft.md
!docs/dogfood-reports/ruview-public-issue-drafts.md
!docs/dogfood-reports/ruview-gitleaks-parse-diagnosis.md
!docs/dogfood-reports/ruview-preinstall-audit-rerun.md
!docs/dogfood-reports/ruview-preinstall-compare.md
!docs/dogfood-reports/ruview-gitleaks-triage.md
!docs/dogfood-reports/working-tree-cleanup-report.md
!docs/dogfood-reports/issue-resolution-sprint-plan.md
!docs/dogfood-reports/current-security-blocker-verification.md
!docs/dogfood-reports/repo-cleanup-audit.md
!docs/dogfood-reports/issue-resolution-batch-plan-current.md
!docs/dogfood-reports/core-application-remediation-sprint.md
!docs/dogfood-reports/rd-self-remediation-dl3018-test.md

# Private operator dogfood (unsanitized — keep local)
docs/DOGFOOD_REPORT_FIRST_39_REPOS.md

# Operator-only closeout scripts (homelab paths, not portable)
scripts/apply_closeout_suppressions.py
scripts/apply_repo_closeout_step2.py
scripts/apply_repo_closeout_step5.py
scripts/issue_closeout_triage.py

# Configuration overrides
config.local.yaml
config.*.local.yaml

# Docker
.dockerignore

# Temporary files
tmp/
temp/
*.tmp

# Coverage reports
coverage.out
coverage.html

# Profiling data
*.prof

# Air live reload
tmp/

# Local development
.local/

# Staged scanner binaries (large; optional for offline Docker builds)
deploy/bin/trivy
deploy/bin/grype
deploy/bin/gitleaks

# Local deployment artifacts and drills (operator-only)
deployment-backups/
restore-drill-test/
config/config.yaml.bak-*

# Root-level build outputs (use build/ or Docker images)
/repository-detective
/repository-detective-build

# Vendored modules (generate via scripts/vendor-deps.sh when needed)
vendor/

# Production configuration files (contain secrets)
docker-compose.prod.yml
docker-compose.override.yml
.env
