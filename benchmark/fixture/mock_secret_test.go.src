package fixture_test

import "testing"

const mockToken = "sk-test-mock-not-real-token-for-unit-tests"

func TestPlaceholderUsesMockToken(t *testing.T) {
	if mockToken == "" {
		t.Fatal("expected mock")
	}
}
